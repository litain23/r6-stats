package org.example.springboot.domain.weeklyoperator;

import org.springframework.data.jpa.repository.JpaRepository;

public interface WeeklyOperatorRepository extends JpaRepository<WeeklyOperator, Long> {
}
